{
  "name": "atlas-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "migrate": "node scripts/migrate.js && node scripts/curator-state.mjs import",
    "curator:export": "node scripts/curator-state.mjs export",
    "curator:import": "node scripts/curator-state.mjs import",
    "snapshot": "node scripts/publish.js",
    "preview": "node scripts/serve-dist.js",
    "test": "node --test"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1"
  }
}
