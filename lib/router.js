// Returns the API route table. `db` is wired in now so route handlers added in
// later tasks can close over the database connection. Empty until then.
export function apiRoutes(db) {
  return [];
}
